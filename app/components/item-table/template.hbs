{{#if searchResults.data}}
<div class="container">
    <form class="form-inline m-b-md" style='text-align: center;'>
        {{input class="form-control search-bar" value=searchInput placeholder="Ex: \"Lorem\""}}
        <div {{action 'search'}} class="btn btn-primary"><i class="fa fa-search" aria-hidden="true"></i></div>
    </form>
    <table class="coll-table m-b-md">
        <thead style="{{theadStyle}}">
            <tr>
                <th>Title</th>
                <th>Author</th>
                <th>Type</th>
            </tr>
        </thead>
        <tbody>
            {{#each searchResults.data as |r|}}
            <tr>
                <td>
                    {{#link-to "collections.collection.item" model.id r.id}}
                    {{r.attributes.title}}
                    {{/link-to}}
                </td>
                <td>{{r.attributes.created-by}}</td>
                <td>{{r.attributes.category}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    {{#if (not-eq totalPages 1) }}
    <div class="pagination-wrapper text-center m-b-md">
        <ul class="pagination">
          <li class="arrow prev prevPage" >
            <a class="btn btn-default " {{action 'prevPage'}}>«</a>
        </li>
        {{#each pageNumberButtons as |item|}}
        <li class="page-number"{{action 'loadPage' item}} data-id="{{item}}"><a>{{item}}</a></li>
        {{/each}}
        <li class="arrow next nextPage">
            <a class="btn btn-default " {{action 'nextPage'}}>»</a>
        </li>
        </ul>
    </div>
    {{/if}}
</div>
{{else}}
    <p class="p-lg text-center"><em>The table cannot be displayed. To display the schedule, add items to this collection and make sure they're indexed.</em></p>
{{/if}}
