<style>
    #ok {
        text-align: justify;
        text-justify: inter-word;
        word-break: break-all;
    }
    .landing-paragraph-container .container {

    }
</style>
<div id="{{get-section-id layout.title}}" class="landing-paragraph-container p-xl"
     style="{{containerStyle}}">
    <div class="container">
            {{#if layout.title}}
                <h2>
                    {{layout.title}}
                    <a class="headerlink" href="#{{get-section-id layout.title}}">
                        <i class="fa fa-link"></i>
                    </a>
                </h2>
            {{/if}}
            <p id="ok" class="text-left">{{layout.body}}</p>
    </div>
</div>
