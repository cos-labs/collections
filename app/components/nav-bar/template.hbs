<div class="coll-navbar" style="background-color:{{model.settings.branding.colors.primary}}">
    <div class="container">
        <div class="coll-navbar-inner">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                {{#link-to "collections.collection" model.id class="navbar-brand"}}{{model.title}}{{/link-to}}
            </div>
            <div class="collapse navbar-collapse" id="myNavbar">
                <div class="navbar navbar-right">
                    <ul class="nav navbar-nav">
                        {{!-- {{#if session.isAuthenticated}}
                            <li>{{#link-to "collections.collection.add"}}Submit {{/link-to}}</li>
                        {{/if}} --}}
                        <li>{{#link-to "collections.collection.browse"}} Browse {{/link-to}}</li>
                        {{#if session.isAuthenticated}}
                            <li>{{#link-to "collections.collection.edit"}} Edit{{/link-to}}</li>
                        {{/if}}
                        <li>{{#link-to "collections.collection.submissions"}} My Submissions {{/link-to}}</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
