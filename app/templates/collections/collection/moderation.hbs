<div class="hero">
    <h1>Moderation Home</h1>
</div>
<div class="text-center" style=" margin-top: 15px; margin-bottom: 15px;">
    All existing submissions {{collection.title}}. In-progress submissions may be continued by
    pressing the continue button.
</div>
<div class="container">
    <table class="table table-striped">
        <thead>
        <tr>
            <th>Submission Name</th>
            <th>Status</th>
            <th class="text-right">Actions</th>
        </tr>
        </thead>
        <tbody>
        {{#each items as |item|}}
            <tr>
                <td>{{item.title}}</td>
                <td>{{item.status}}</td>
                <td class="text-right">
                    {{#link-to 'collections.collection.items.item.index' collection.id item.id
                               class="btn btn-default"}}View Item{{/link-to}}
                    {{#link-to 'collection.collections.items.item.edit' collection.id item.id
                               class="btn btn-default"}}Edit Item{{/link-to}}
                </td>
            </tr>
        {{else}}
            <div class="empty-state">
                You have not started any submissions to {{collection.title}}
            </div>
        {{/each}}
        </tbody>
    </table>
</div>