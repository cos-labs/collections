<section class="coll-banner text-center">
    <h1 class="f-w-lg"> Browse Collections </h1>
    <p class="lead f-w-md">Organize your research interests in a way that makes sense</p>
    {{#if session.isAuthenticated}}
        {{#link-to 'create' class="btn btn-success btn-lg m-t-lg"}}Start a collection {{/link-to}}
    {{/if}}
</section>

<div class="results">

    <div class="search">
    
        {{input class="searchbox" placeholder="Search..." value=q}}
        <button class="btn btn-success" {{action "doQuery" q}}>Search</button>
    
    </div>

    <nav class="results-nav">
        <a>Kind</a>
        <a>Creator</a>
        <a>Date Created</a>
    </nav>

    <div class="result-items">

        {{#each collections as |collection|}}
            <div class="result-item">

                <h2>
                    {{#link-to "collections.collection" collection.id}}
                        <span>
                            {{collection.title}}
                        </span>
                    {{/link-to}}
                </h2>
                <div class="description">{{collection.description}}</div>

            </div>
        {{/each}}

    </div>


</div>
